/**
 * Design Tokens - Central Color System
 *
 * Usage:
 * - Import primitiveColors for raw color values
 * - Import semanticColors for themed, role-based colors
 *
 * Example:
 * import { primitiveColors, semanticColors } from '@/styles/tokens';
 */

export { primitiveColors, type PrimitiveColors } from "./colors";
export { semanticColors, type SemanticColors } from "./semantic";
